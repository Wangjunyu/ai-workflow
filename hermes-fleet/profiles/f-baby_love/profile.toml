name = "f-baby_love"
description = "小肥肥助手 — 循证育儿记录员+参谋，群聊静默收集→私聊报告"
layers = ["cli"]
required_secrets = ["DEEPSEEK_API_KEY"]

[distribution]
default_targets = ["firefly"]

[runtime_policy]
sync_sessions = false
sync_logs = false
sync_memory = "local_only"
